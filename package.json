{
  "scripts": {
    "build": "postcss styles.css -o src/styles.css",
    "build-docker": "docker build -t minimal-portfolio .",
    "start-docker": "docker run -d -p 443:443 --name minimal-portfolio minimal-portfolio"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.15",
    "daisyui": "^4.12.10",
    "tailwindcss": "^3.4.10"
  },
  "dependencies": {
    "autoprefixer": "^10.4.20",
    "postcss-cli": "^11.0.0"
  }
}
